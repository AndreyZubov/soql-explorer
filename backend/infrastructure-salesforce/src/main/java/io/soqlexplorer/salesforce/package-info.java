/**
 * Salesforce adapter module — placeholder for Step 1.
 *
 * <p>Implementations of {@code SalesforceGatewayPort} and {@code TokenCipherPort} land here in
 * Step 2. The empty package-info exists so the module compiles to a non-empty jar.
 */
package io.soqlexplorer.salesforce;
