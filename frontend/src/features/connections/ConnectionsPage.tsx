// Placeholder — full implementation lands in Step 2.
export default function ConnectionsPage() {
  return (
    <section>
      <h1 className="text-lg font-semibold">Connections</h1>
      <p className="text-sm text-muted-foreground">
        Salesforce connection management arrives in Step 2 of the implementation plan.
      </p>
    </section>
  );
}
